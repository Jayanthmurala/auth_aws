# =============================================================================
# .dockerignore - Nexus Auth Service
# Excludes files from Docker build context for faster builds and smaller images
# =============================================================================

# Dependencies (will be installed in container)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
package-lock.json

# Build outputs (will be built inside container)
dist/
build/
*.tsbuildinfo

# Environment files (will be managed separately)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Database files
*.db
*.sqlite
*.sqlite3

# Logs
logs/
*.log
pids/
*.pid
*.seed
*.pid.lock

# Coverage and test outputs
coverage/
*.lcov
.nyc_output

# Development and documentation files
.git/
.gitignore
README.md
*.md
.vscode/
.idea/
.editorconfig

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Temporary files
*.tmp
*.temp

# Prisma migrations (migrations run separately, not in container)
prisma/migrations/

# Test files (not needed in production)
tests/
*.test.ts
*.spec.ts
vitest.config.ts

# Development tools and scripts
auth-load-tests/
scripts/seed-superadmin.ts
scripts/promote.ts
scripts/check-connection.ts

# Generated keys (will be set via environment variables)
generated_keys.env

# Docker files (not needed inside container)
Dockerfile*
docker-compose*.yml
.dockerignore

# AWS deployment files (not needed in container)
aws-deployment/

# CI/CD files
.github/
.gitlab-ci.yml
.travis.yml

# Documentation
docs/
*.md
